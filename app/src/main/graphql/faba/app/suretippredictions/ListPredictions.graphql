query ListPredictions($date: String!, $nextTokenPred: String!, $nextTokenOdd: String!) {
    listPredictions(predictionDate: $date, nextToken: $nextTokenPred) {
        items {
            id
            homeLogo
            homeName
            awayLogo
            awayName
            predictions
            predictionDate
            gameTime
        }
        nextToken
    }

    listOdds(oddDate: $date, nextToken: $nextTokenOdd) {
        items {
            bookmaker
            fixture
            id
            oddDate
        }
        nextToken
    }
}